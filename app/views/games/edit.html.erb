

 <div class="row">
  <div class= "col-xs-4">
  	<h1>Scoring</h1>
<%= render 'form' %>

<%= link_to 'New Round P1', new_game_path(:player => 1) %>
<br>
<%= link_to 'New Round P2', new_game_path(:player => 2) %>
<br>
<%= link_to 'Back', games_path %>
</div>

<% ["one","two"].each do |player| %>
<div class = "col-xs-4">
	<br>
  <div class="btn-group">
	<% [-2500,-500,500,1000,1500,2000,3000,4000,5000].each do |value| %>

  	<%= link_to "give player #{player} #{value}", score_game_path(:id => @game.id, :player => player, :value => value), {:class => "btn btn-default btn-block" } %>
  	<br />
	<% end %>
  </div>
</div>
<% end %>

</div>